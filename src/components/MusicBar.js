import React, { Component } from 'react';
import './MusicBar.css';

class MusicBar extends Component {
  render() {
    return (
      <div>
        
      </div>
    );
  }
}

export default MusicBar;
